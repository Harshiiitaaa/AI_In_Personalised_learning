# -----------------------------------------------------------------------------
# EXAMPLE ENVIRONMENT VARIABLES
#
# Copy this file to .env and fill in the actual values.
# Do NOT commit the .env file to your version control system.
# -----------------------------------------------------------------------------

# FastAPI settings
# Generate a secret key using: openssl rand -hex 32
SECRET_KEY=your_super_secret_key_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Mongo
MONGO_URL=mongodb://localhost:27017/
MONGO_DB=mydatabase

# Judge0 - Code Execution Engine
# You can use the public RapidAPI instance or host your own.
JUDGE0_URL=https://judge0-ce.p.rapidapi.com
JUDGE0_KEY=your_rapidapi_key_for_judge0
JUDGE0_HOST_HEADER=judge0-ce.p.rapidapi.com

# AI API Keys (uncomment and use only one)
# OPENAI_API_KEY=your_openai_api_key
GEMINI_API_KEY=your_gemini_api_key

# CORS (Comma-separated list of allowed origins)
ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# Redis / Celery (for background tasks)
REDIS_URL=redis://localhost:6379/0

# Email (optional; if not set, reminders might be logged to console instead)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASSWORD=supersecretpassword
SMTP_FROM=noreply@yourapp.com